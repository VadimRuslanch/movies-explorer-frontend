import { timeShortFilm } from "../utils/constants"

export default function useFilterMovies(movies, text) {
    const filer = movies.filter((i) => {
        // console.log(text);
        // const res = i.nameEN.startsWith(text);
        // return console.log(res)
    })
    // console.log(filer);
}